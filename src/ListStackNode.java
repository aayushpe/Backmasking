public class ListStackNode {
    protected double value; // Value the node holds
    protected ListStackNode next; // The Next Value it points to

    public ListStackNode(double d){
        value = d;
        next = null; // The node always points to null unless otherwise stated
    }
}

